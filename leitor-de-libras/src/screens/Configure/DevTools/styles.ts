import { StyleSheet } from "react-native";
import { ThemeProps } from "../../../theme/getTheme";

interface DevToolsStyle {
    colors: ThemeProps;
}

export default ({ colors }: DevToolsStyle) => StyleSheet.create({

});
