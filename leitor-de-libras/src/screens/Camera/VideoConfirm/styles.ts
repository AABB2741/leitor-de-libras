import { StyleSheet } from "react-native";
import { ThemeProps } from "../../../theme/getTheme";

interface VideoConfirmStyle {
    colors: ThemeProps;
}

export default ({ colors }: VideoConfirmStyle) => StyleSheet.create({

});
