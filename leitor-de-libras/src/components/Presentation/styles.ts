import { StyleSheet } from "react-native";
import { ThemeProps } from "../../theme/getTheme";

interface PresentationStyle {
    colors: ThemeProps;
}

export default ({ colors }: PresentationStyle) => StyleSheet.create({

});
